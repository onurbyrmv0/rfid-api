# SERVER CONFIGURATION (Used by Docker/Flask)
# POSTGRES_HOST MUST be "db" (Docker internal hostname) - Do not change this to an IP!
POSTGRES_USER=admin
POSTGRES_PASSWORD=adminpass
POSTGRES_DB=attendance_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Admin Panel Credentials
ADMIN_USER=admin
ADMIN_PASS=admin123

# API Security
API_KEY=secret_api_key_123

# CLIENT CONFIGURATION (Used by Raspberry Pi)
# Cloudflare/Nginx Reverse Proxy is set up, so use the HTTPS domain
SERVER_URL=https://rfid.onurbayramov.codes
